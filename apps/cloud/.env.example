# SvelteKit loads env from the monorepo root (see apps/cloud/vite.config.js).
# Copy the root .env.example to ../../.env or set variables in your shell.

DATABASE_URL=postgres://postgres:YOUR_PASSWORD@localhost:5432/cco_apps
PGSCHEMA=esr_cloud
