# Copy this file to `.env` at the monorepo root (ESR-APP/.env)
# Used by: pnpm dev:cloud, pnpm db:postgres:migrate, pnpm db:postgres:seed

DATABASE_URL=postgres://postgres:YOUR_PASSWORD@localhost:5432/cco_apps
PGSCHEMA=esr_cloud
PGSSL=false
PGPOOL_MAX=10
