# ESR Cloud

Antes de tocar cualquier archivo de UI, lee `DESIGN_SYSTEM.md` y aplícalo.
Si un cambio requiere un color, tamaño o patrón que no está ahí, propónlo como
token nuevo en ese archivo en vez de inventarlo en el componente.